import { View, Text, StyleSheet } from 'react-native';

const ManageQuote = () => {
  return (
    <View style={styles.container}>
      <Text>ManageQuote Screen</Text>
    </View>
  );
};

export default ManageQuote;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#202120',
  },
});
